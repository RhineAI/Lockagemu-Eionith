using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class projectormesh : MonoBehaviour
{
    /*[SerializeField] private proyeksirute projector;
    [SerializeField] public float ketebalanmesh;

    public Mesh mesh;
    public MeshFilter meshfilter;

    private void Awake()
    {
        if (projector == null) projector.GetComponent<proyeksirute>();
        meshfilter.GetComponent<MeshFilter>();

        transform.position = Vector3.zero;
    }
    // Start is called before the first frame update
    void Start()
    {
        transform.position = projector.transform.position;

        UpdateMesh();

        projector.
    }

    private void projector_ondirty(object sender, Even)

    private void UpdateMesh()
    {
        if(mesh != null)
        {
            mesh.Clear();
            Destroy(mesh);
            mesh = null;
        }

        List<Projector.point> pointlist = projector.getpo
    }

    // Update is called once per frame
    void Update()
    {
        
    }*/
}
